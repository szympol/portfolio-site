import React from "react";

const Hero = () => {
    return (
        <header className="hero component first-component" id="hero">
            <span>Hero</span>
        </header>
    )
}

export default Hero;
